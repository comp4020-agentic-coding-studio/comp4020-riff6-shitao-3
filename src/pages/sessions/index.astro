---
import ContentLayout from "astro-theme-university/layouts/ContentLayout.astro";
import SessionsGrid from "../../components/SessionsGrid.astro";
import { sessionLabels, siteConfig } from "../../site-config";
---

<ContentLayout
  title={sessionLabels.plural}
  description={`The twelve-week teaching schedule: what happens in each ${sessionLabels.singular.toLowerCase()}, what to prepare, and what leaves the room.`}
  heroImage="/src/assets/shitao/sessions-outing.jpg"
  heroImageAlt={`"Outing to Zhang Gong's Grotto," an album leaf by Shitao (石涛, 1642 – c. 1707), ink and colour on paper. Metropolitan Museum of Art, public domain.`}
  {...siteConfig}
>
  <p>
    Twelve studios, each one working mechanism at a time — a pointer signal,
    a constraint, a decay, a second hand on the same instrument — building
    toward the single idea the semester closes on: 无法而法，乃为至法, the
    method of no method is the perfect method.
  </p>
  <SessionsGrid />
</ContentLayout>
